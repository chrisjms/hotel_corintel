# =====================================================
# Root .htaccess - Security only
# Each surface (client/, admin/, superadmin/, vitrine/) has its own .htaccess
# =====================================================

Options -Indexes

RewriteEngine On

# Block access to shared/ directory (should never be served directly)
RewriteRule ^shared/ - [F,L]
